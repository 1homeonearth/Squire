{
  "discord_token": "$ENV{DISCORD_TOKEN}",
  "application_id": "$ENV{DISCORD_APPLICATION_ID}",
  "public_key": "$ENV{DISCORD_PUBLIC_KEY}",
  "database_path": "data/squire.db",
  "feature_flags": {
    "xp": true,
    "moderation": true,
    "logging": true
  }
}
