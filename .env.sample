# Environment values for the Rust-based Squire bootstrap
# Copy to .env (kept out of source control) and replace placeholders.

# Discord credentials are referenced from config.json using the $ENV{...} syntax.
DISCORD_TOKEN=REPLACE_ME
DISCORD_APPLICATION_ID=REPLACE_ME
DISCORD_PUBLIC_KEY=REPLACE_ME
