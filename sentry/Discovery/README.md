# Discovery directory — Sentry

Presence markers (`ecosystem_presence.txt`) and message queues (`gateway_queue.log`) live here when Sentry is enrolled by an ecosystem. Keep this directory free of secrets.
