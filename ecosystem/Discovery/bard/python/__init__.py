"""
Bard package marker.

This short note marks `bard/python` as a package so relative imports work even
before modules are filled in. Keeping this file in place prevents import errors
when Bard is moved into an ecosystem’s `Discovery/` folder or nested inside
another bot.
"""
